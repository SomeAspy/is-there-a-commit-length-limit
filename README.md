# is-there-a-commit-length-limit
first commit is a base64 png
lets see

This commit is a 1.1 MB video.

Files can be extracted (since the web UI shows ... at the end) with the following command
`git log --format=%B [commit hash]`
and then by converting back to their respective format.

`44de36374993b2c2cdb8392c09b81a49b3b74ab7` is a PNG file of 204kB
This commit is a MP4 file of 1.1MB

